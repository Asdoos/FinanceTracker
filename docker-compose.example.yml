services:
  finance-tracker:
    image: anri04/finance-tracker:latest
    ports:
      - "3001:3001"
    volumes:
      - finance-data:/data
    environment:
      - DATABASE_PATH=/data/finance.db
    restart: unless-stopped

volumes:
  finance-data:
