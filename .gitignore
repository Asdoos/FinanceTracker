node_modules
dist
.env.local
.env
docker-compose.yml

# Convex auto-generated files (run `npx convex dev` to regenerate)
# convex/_generated/ — stubs are committed for CI/Docker builds

# Personal seed data
convex/seed.ts

# Editor
.vscode
.idea

# Claude Code
.claude/settings.local.json
